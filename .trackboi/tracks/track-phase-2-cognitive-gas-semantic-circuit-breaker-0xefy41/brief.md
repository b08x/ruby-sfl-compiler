## Problem\n\nThe circuit breaker in `PassTwoEngine` (`lib/sfl/compiler/pass_two/pass_two_engine.rb` around line 173) is a no-op pass-through — `lambda { |&block| block.call }`. The `rescue CircuitBreaker::CircuitBrokenException` clause will never trigger. Phase 1's implicit circuit breaker was the Gödel BIGINT overflow (a database crash). Phase 2 needs a deliberate, semantically-grounded replacement.\n\n## Pattern\n\nCognitive Gas allocates a semantic budget per reasoning loop, not a token count. SFL-derived clause cost weights:\n\n- **Process complexity**: material + mental processes cost more than relational/existential (they describe actions and mental states requiring more evaluation)\n- **Modality density**: high-modality clauses (strong certainty claims) cost more — evaluating a certainty claim requires verify-or-reject reasoning\n- **Tenor shifts**: a clause diverging significantly from the running average costs more — requires context-switch to process\n\nWhen budget exhaustion approaches, the system forces graceful summarization (Rolling Synthesis cycle) rather than crashing or silently truncating.\n\n## What it replaces\n\n- `PassTwoEngine#default_circuit_breaker` no-op lambda\n- `CircuitBreaker::CircuitHandler` placeholder (never injects a real breaker today)\n- Phase 1 failure mode: BIGINT crash = uncontrolled process death\n\n## Key files\n\n- `lib/sfl/compiler/pass_two/pass_two_engine.rb` — inject real breaker\n- New: `lib/sfl/compiler/cognitive_gas.rb` — budget model, cost function, exhaustion callback\n- New: `spec/sfl/compiler/cognitive_gas_spec.rb`\n\n## What it preserves\n\nThe degradation ladder. `annotation_source: \"llm\" | \"fallback\" | \"stub\"` provenance carries through summarized axioms. Downstream consumers assess compressed evidence reliability the same way they assess raw clauses today.
